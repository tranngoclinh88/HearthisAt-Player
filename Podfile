platform :ios, '10.0'

target 'HearthisAt-Player' do
  use_frameworks!

  # Pods for HearthisAt-Player
  pod 'Alamofire', '~> 4.5.0'
  pod 'ObjectMapper', '~> 2.2.0'
  pod 'SwiftyJSON', '~> 3.1.0'

end
